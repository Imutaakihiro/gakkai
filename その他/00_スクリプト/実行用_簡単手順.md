# Windows PCでのマルチタスク学習実行手順（簡易版）

## 🚀 実行手順（コピペで実行）

### ステップ1: 仮想環境を有効化

```powershell
cd "C:\Users\mkmzk\Desktop\gakkai\その他"
.\venv\Scripts\Activate.ps1
```

### ステップ2: PyTorch（GPU版）をインストール

```powershell
python -m pip uninstall torch torchvision torchaudio -y
python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### ステップ3: その他のパッケージをインストール

```powershell
python -m pip install transformers pandas "numpy<2.0.0" scikit-learn matplotlib seaborn tqdm "shap>=0.42.0"
```

### ステップ4: GPUの確認

```powershell
python -c "import torch; print('CUDA available:', torch.cuda.is_available()); print('GPU:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'N/A')"
```

### ステップ5: マルチタスク学習を実行

```powershell
cd "C:\Users\mkmzk\Desktop\gakkai\その他\00_スクリプト"
python train_class_level_ordinal_llp.py
```

## ⏱️ 予想実行時間

- **NVIDIA GPU使用時**: 30-45分（GPU性能により変動）
- **CPUのみ**: 1.5-3時間（非推奨）

## ⚠️ 注意事項

- 仮想環境が有効化されていることを確認（プロンプトに `(venv)` が表示されている）
- GPUが利用できない場合は、CPUで実行されますが時間がかかります
