---
alwaysApply: true
---

# 理系大学生 LaTeX論文執筆ルール

**目的**: 権威のある教授に見せても恥じない、高品質なLaTeX論文を執筆するための必須ルール

---

## 📋 基本原則

### 1. LaTeX文書の構成

LaTeX論文は必ず以下の構成に従うこと：

```latex
\documentclass[a4paper,11pt,twocolumn]{jsarticle}  % または ltjsreport
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
% ... その他のパッケージ ...

\begin{document}
\maketitle
\section{はじめに}
% ...
\section{研究方法}
% ...
\section{実験結果}
% ...
\section{おわりに}
% ...
\begin{thebibliography}{9}
% ...
\end{thebibliography}
\end{document}
```

### 2. ドキュメントクラスの選択

#### jsarticle（論文・予稿用）
- **用途**: 学会予稿、短い論文
- **特徴**: 2カラム対応、ページ数制限あり
- **使用例**: `\documentclass[a4paper,11pt,twocolumn]{jsarticle}`

#### ltjsreport（卒業論文用）
- **用途**: 卒業論文、長い論文
- **特徴**: 1カラム、章立て対応
- **使用例**: `\documentclass[a4paper,10.5pt]{ltjsreport}`
- **注意**: LuaLaTeX専用（pdfLaTeXでは使用不可）

#### jsreport（卒業論文用・pdfLaTeX対応）
- **用途**: 卒業論文（pdfLaTeX使用時）
- **特徴**: 1カラム、章立て対応
- **使用例**: `\documentclass[a4paper,10.5pt]{jsreport}`
- **注意**: pLaTeX/uplatex専用

### 3. 必須パッケージ

#### 基本パッケージ
```latex
\usepackage[utf8]{inputenc}        % UTF-8エンコーディング
\usepackage{amsmath}               % 数式環境
\usepackage{amsfonts}              % 数学フォント
\usepackage{amssymb}               % 数学記号
\usepackage{booktabs}              % 美しい表（横線のみ）
\usepackage{geometry}              % ページレイアウト
\usepackage{float}                 % 図表の配置
\usepackage{hyperref}              % ハイパーリンク
```

#### 日本語処理（pdfLaTeX用）
```latex
\usepackage{zxjafont}              % 日本語フォント
\usepackage{pxchfon}               % フォント設定
```

#### 日本語処理（LuaLaTeX用）
```latex
\usepackage[no-math]{luatexja-preset}  % 日本語処理
```

#### 図表用パッケージ
```latex
\usepackage{graphicx}              % 図の挿入
\usepackage{array}                  % 表の拡張
\usepackage{adjustbox}              % 図表の調整
```

#### URL・改行用パッケージ
```latex
\usepackage{url}                    % URL表示
\usepackage{xurl}                   % URL改行
\usepackage{seqsplit}               % 長い文字列の分割
```

### 4. 文体・表現

#### 必須事項
- **文語体「である」調**を使用（口語体は厳禁）
- **客観的で簡潔な表現**を心がける
- **主語を明確に**する（「本研究では」「実験により」など）
- **推測と事実を区別**する（「と考えられる」「と示された」など）

#### LaTeXでの強調
```latex
\textbf{重要}          % 太字
\emph{強調}            % 斜体（強調）
\textit{斜体}          % 斜体
```

#### 禁止事項
- ❌ 口語体（「〜だ」「〜です」）
- ❌ 主観的な表現（「〜だと思う」「〜かもしれない」）
- ❌ 曖昧な表現（「かなり」「非常に」などは数値で示す）
- ❌ 一人称（「私は」「私たちは」→「本研究では」）

#### 数値の記述
- **有効数字を統一**する（例：0.123、12.3、$1.23 \times 10^2$）
- **統計的有意性を必ず明記**（$p$値、信頼区間など）
- **単位を必ず記述**する（例：3.459点、0.216標準偏差）
- **範囲を示す場合は明確に**（例：3.330～3.600点）

---

## 📊 図表の扱い（LaTeX版）

### 図（Figure）のルール

#### 1. 図の挿入方法

```latex
\begin{figure*}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{figure1.pdf}
\caption{感情スコアと授業評価スコアの散布図（n=3,268授業、r=0.3097、$p < 0.000001$）}
\label{fig:correlation}
\end{figure*}
```

**重要ポイント:**
- `figure*`環境: 2カラム形式で図を全幅表示
- `[htbp]`: 配置オプション（h=here, t=top, b=bottom, p=page）
- `\centering`: 中央揃え
- `\includegraphics`: 図の挿入（PDF、PNG、JPG対応）
- `\caption`: キャプション（図の下）
- `\label`: 参照用ラベル（`\ref{fig:correlation}`で参照）

#### 2. 図の品質要件

- **解像度は300dpi以上**（印刷品質）
- **ベクター形式（PDF、SVG）を推奨**
- **文字サイズは読める大きさ**（10pt以上推奨）
- **軸ラベル、凡例、単位を必ず明記**

#### 3. 図のサイズ指定

```latex
\includegraphics[width=0.8\textwidth]{figure1.pdf}      % 本文幅の80%
\includegraphics[width=0.5\columnwidth]{figure1.pdf}   % カラム幅の50%（2カラム用）
\includegraphics[height=5cm]{figure1.pdf}              % 高さ5cm
\includegraphics[scale=0.8]{figure1.pdf}                % 80%に縮小
```

### 表（Table）のルール

#### 1. 表の挿入方法

```latex
\begin{table*}[htbp]
\centering
\caption{授業評価スコアの基本統計量（n=3,268授業）}
\label{tab:stats1}
\small
\begin{tabular}{lc}
\toprule
統計量 & 値 \\
\midrule
平均 & 3.459 \\
標準偏差 & 0.216 \\
最小値 & 2.000 \\
第1四分位数（Q1） & 3.330 \\
中央値（Q2） & 3.480 \\
第3四分位数（Q3） & 3.600 \\
最大値 & 4.000 \\
\bottomrule
\end{tabular}
\end{table*}
```

**重要ポイント:**
- `table*`環境: 2カラム形式で表を全幅表示
- `\caption`: キャプション（表の上）
- `\label`: 参照用ラベル（`\ref{tab:stats1}`で参照）
- `booktabs`パッケージ: `\toprule`, `\midrule`, `\bottomrule`で美しい表
- `\small`: 表の文字サイズを小さく（必要に応じて）

#### 2. 表の列指定

```latex
\begin{tabular}{lcr}     % l=左揃え, c=中央揃え, r=右揃え
\begin{tabular}{|l|c|r|} % |で縦線を追加（非推奨）
\begin{tabular}{p{3cm}}  % 固定幅の段落
```

#### 3. 表の罫線

**推奨（booktabsパッケージ）:**
```latex
\toprule    % 上線
\midrule    % 中間線
\bottomrule % 下線
```

**非推奨（縦線は最小限）:**
```latex
\hline      % 横線（booktabsの方が美しい）
|           % 縦線（使わない方が良い）
```

### 図表の参照

#### 本文中での参照

```latex
図\ref{fig:correlation}に示すように，感情スコアと授業評価スコアには相関関係がある．
表\ref{tab:stats1}の通り，授業評価スコアの平均は3.459点であった．
```

**重要ポイント:**
- `\ref{label}`: 自動番号参照（「図1」「表1」など）
- 図表番号は本文中で初めて言及する際に引用
- 必ず本文中で参照する（参照されていない図表は削除）

---

## 🔢 数式の扱い（LaTeX版）

### 数式環境の選択

#### 1. インライン数式

```latex
相関係数は$r = 0.3097$であった．
期待値は$E[y] = 1 \times P_1 + 2 \times P_2 + 3 \times P_3 + 4 \times P_4$である．
```

**重要ポイント:**
- `$...$`: インライン数式（本文中に埋め込む）
- 日本語と数式の間はスペース不要（LaTeXが自動調整）

#### 2. 独立した数式（番号なし）

```latex
\begin{equation*}
E[y] = 1 \times P_1 + 2 \times P_2 + 3 \times P_3 + 4 \times P_4
\end{equation*}
```

#### 3. 独立した数式（番号付き）

```latex
\begin{equation}
P_1 = 1 - p_{\geq 2}, \quad P_2 = p_{\geq 2} - p_{\geq 3}, 
\quad P_3 = p_{\geq 3} - p_{\geq 4}, \quad P_4 = p_{\geq 4}
\label{eq:probability}
\end{equation}
```

**本文中での参照:**
```latex
式\eqref{eq:probability}に示すように，各クラスの確率を復元する．
```

#### 4. 複数行の数式

```latex
\begin{align}
P_1 &= 1 - p_{\geq 2} \\
P_2 &= p_{\geq 2} - p_{\geq 3} \\
P_3 &= p_{\geq 3} - p_{\geq 4} \\
P_4 &= p_{\geq 4}
\end{align}
```

### 数式の記述ルール

#### 1. 数式番号
- 重要な数式には必ず番号を付与
- `\begin{equation}`環境を使用
- `\label{eq:name}`でラベルを付与
- 本文中で`\eqref{eq:name}`で参照

#### 2. 変数の定義
```latex
$E[y] = 1 \times P_1 + 2 \times P_2 + 3 \times P_3 + 4 \times P_4$
ここで，$E[y]$は期待値，$P_i$は評価スコア$i$の確率である（$i \in \{1, 2, 3, 4\}$）．
```

#### 3. 記号の統一
- 同じ概念には同じ記号を使用
- 記号一覧表を作成（必要に応じて）
- ギリシャ文字: `\alpha`, `\beta`, `\gamma`, `\delta`, `\theta`, `\lambda`, `\mu`, `\sigma`, `\phi`, `\psi`, `\omega`

#### 4. 単位の明記
```latex
$R^2 = 0.392$（39.2\%の分散を説明）
相関係数は$r = 0.3097$（$p < 0.000001$，統計的に極めて有意）であった．
```

### よく使う数式コマンド

```latex
\frac{a}{b}           % 分数 a/b
\sqrt{x}              % 平方根
\sum_{i=1}^{n}        % 総和
\prod_{i=1}^{n}       % 総積
\int_{a}^{b}          % 積分
\lim_{n \to \infty}   % 極限
\partial              % 偏微分記号
\nabla                % ナブラ
\vec{v}               % ベクトル
\dot{x}               % 時間微分
\ddot{x}              % 2階時間微分
```

---

## 📚 参考文献の扱い（LaTeX版）

### 引用のルール

#### 1. 本文中での引用

```latex
BERT\cite{bert}は双方向のTransformerエンコーダーから構成される．
SHAP\cite{shap}は協力ゲーム理論のShapley値を機械学習に応用した手法である．
```

**重要ポイント:**
- `\cite{label}`: 参考文献を引用
- 複数引用: `\cite{bert,shap,multitask}`
- 引用は句読点の前

#### 2. 参考文献リストの記述

```latex
\begin{thebibliography}{9}

\bibitem{bert} Devlin, J., Chang, M. W., Lee, K., \& Toutanova, K. (2018). 
BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding. 
arXiv preprint arXiv:1810.04805.

\bibitem{shap} Lundberg, S. M., \& Lee, S. I. (2017). 
A Unified Approach to Interpreting Model Predictions. 
Advances in Neural Information Processing Systems, 30, pp. 4765-4774.

\end{thebibliography}
```

**重要ポイント:**
- `\begin{thebibliography}{9}`: 最大9件の参考文献（必要に応じて変更）
- `\bibitem{label}`: 参考文献のエントリ
- `\&`: アンパサンド（&）をエスケープ
- 形式は統一（著者名, タイトル, 雑誌名, 年）

#### 3. BibTeX/BibLaTeXの使用（推奨）

**BibTeXファイル（references.bib）:**
```bibtex
@article{bert2018,
  author = {Devlin, Jacob and Chang, Ming-Wei and Lee, Kenton and Toutanova, Kristina},
  title = {BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding},
  journal = {arXiv preprint arXiv:1810.04805},
  year = {2018}
}
```

**LaTeXファイル:**
```latex
\bibliographystyle{ieeetr}  % または jplain, junsrt など
\bibliography{references}
```

**コンパイル手順:**
```bash
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

### 参考文献の品質

- **一次資料（原著論文）を優先**
- **信頼性の高いソース**（査読付き論文、学術書籍）
- **最新の研究**を参照（可能な限り5年以内）
- **Webサイトは最小限**（必要に応じてアクセス日を明記）

---

## 📈 データと統計の扱い（LaTeX版）

### データ提示のルール

#### 1. 基本統計量の明記

```latex
平均3.459点（標準偏差0.216，$n=3,268$授業）であった．
相関係数は$r = 0.3097$（$p < 0.000001$，統計的に極めて有意）であった．
```

#### 2. 統計的有意性の明記

```latex
$p < 0.05$     % 有意水準5%
$p < 0.01$     % 有意水準1%
$p < 0.001$    % 有意水準0.1%
$p < 0.000001$ % 極めて有意
```

#### 3. 効果量の記述

```latex
ピアソン相関係数0.3097は，Cohenの基準\cite{cohen}に基づき
中程度の正の相関（$r = 0.30$～$0.50$）と解釈される．
```

### 統計処理の記述

```latex
ピアソン相関係数を用いて相関分析を実施した．
カイ二乗独立性検定により独立性を検証した．
```

---

## 🎯 実験結果の記述（LaTeX版）

### 結果の記述ルール

#### 1. 客観的な記述

```latex
感情スコアと授業評価スコアの相関係数は$0.3097$であった（$p < 0.000001$）．
授業レベルマルチタスクモデルは$R^2 = 0.106$を達成した．
```

#### 2. 比較の明記

```latex
評価スコア予測の$R^2$が$-0.108$から$0.106$へと大幅に改善した．
相関係数も$0.094$から$0.440$へと約$4.7$倍向上した．
```

#### 3. 失敗や限界の記述

```latex
個人レベルのデータでは$R^2 \approx 0$であり，予測は困難であった．
```

---

## 🔍 品質管理：3回チェックシステム（LaTeX版）

### チェック1: 内容の正確性

1. **数値の確認**
   - すべての数値が正確か
   - 単位が正しいか
   - 計算が正しいか

2. **引用の確認**
   - すべての主張に根拠があるか
   - 参考文献が正しく引用されているか（`\cite{label}`）
   - 引用形式が統一されているか

3. **論理の一貫性**
   - 目的と結果が一致しているか
   - 考察が結果に基づいているか
   - 矛盾がないか

### チェック2: 表現と文体

1. **文体の統一**
   - 「である」調で統一されているか
   - 口語体が混入していないか
   - 主語が明確か

2. **専門用語の使用**
   - 専門用語が正しく使用されているか
   - 略語は初出時に定義されているか
   - 用語が統一されているか

3. **文章の明瞭性**
   - 一文が長すぎないか（50字以内推奨）
   - 主語と述語が対応しているか
   - 修飾関係が明確か

### チェック3: フォーマットと形式（LaTeX特有）

1. **図表の確認**
   - すべての図表に`\caption`があるか
   - すべての図表に`\label`があるか
   - 図表番号が連番か（`\ref`で自動生成）
   - 本文中で`\ref{label}`で参照されているか
   - 解像度が十分か（300dpi以上）

2. **数式の確認**
   - 重要な数式に`\label{eq:name}`が付いているか
   - 変数が定義されているか
   - 本文中で`\eqref{eq:name}`で参照されているか
   - 数式の構文エラーがないか

3. **参考文献の確認**
   - 参考文献リストが完全か
   - `\cite{label}`と`\bibitem{label}`が一致しているか
   - 引用形式が統一されているか
   - BibTeXを使用している場合、`.bib`ファイルが正しいか

4. **LaTeX構文の確認**
   - すべての`\begin{環境}`に対応する`\end{環境}`があるか
   - エスケープが必要な文字（`&`, `%`, `$`, `#`, `^`, `_`, `{`, `}`, `~`, `\`）が正しくエスケープされているか
   - パッケージの競合がないか
   - 未定義のコマンドがないか

5. **コンパイルの確認**
   - エラーなくコンパイルできるか
   - 警告（warning）がないか（可能な限り）
   - PDFが正しく生成されるか
   - ページ番号が正しいか
   - 目次が最新か（`\tableofcontents`を使用している場合）

---

## ⚠️ 倫理的配慮

### 必須事項

1. **研究倫理**
   - データの取得方法が倫理的か
   - 個人情報の保護が適切か
   - 研究参加者への説明と同意が得られているか

2. **学術的誠実性**
   - データの改ざんや捏造をしていないか
   - 他人の研究を適切に引用しているか
   - 利益相反がないか

3. **再現性の確保**
   - 実験手順が詳細に記述されているか
   - データとコードが公開可能か（可能な限り）
   - 使用したソフトウェアとバージョンを明記

---

## 📝 LaTeXフォーマット要件

### 1. 基本設定

#### ページレイアウト（geometryパッケージ）

```latex
\usepackage{geometry}
\geometry{
  left=15mm,
  right=15mm,
  top=25mm,
  bottom=25mm,
  textwidth=180mm,
  textheight=250mm
}
```

#### 2カラム設定

```latex
\documentclass[a4paper,11pt,twocolumn]{jsarticle}
\setlength{\columnsep}{12mm}        % カラム間隔
\setlength{\columnseprule}{0pt}     % カラム間の罫線（0pt=なし）
```

#### 改行とレイアウト設定

```latex
\sloppy                              % 緩い改行
\raggedbottom                        % ページ下部を揃えない
\hyphenpenalty=1000                  % ハイフネーションペナルティ
\exhyphenpenalty=1000                % 例外ハイフネーションペナルティ
\tolerance=2000                      % 許容度
\pretolerance=2000                   % 事前許容度
\setlength{\emergencystretch}{10em} % 緊急ストレッチ
\setlength{\parindent}{1em}         % 段落の字下げ
\setlength{\parskip}{0pt}            % 段落間のスペース
```

### 2. 句読点

- **日本語**: 全角「，」「．」
- **英数字間**: 半角「.」「,」
- **数式内**: 半角「,」「.」

### 3. 見出し

```latex
\chapter{はじめに}           % 章（ltjsreport, jsreportのみ）
\section{研究の背景}         % 節
\subsection{教育の質向上の重要性}  % 項
\subsubsection{現状の課題}   % 小項
\paragraph{具体例}          % 段落見出し
```

**見出しのスタイル設定:**
```latex
\usepackage{titlesec}
\titleformat*{\section}{\normalsize\bfseries}
\titleformat*{\subsection}{\normalsize\bfseries}
\titleformat*{\subsubsection}{\normalsize\bfseries}
```

### 4. ページ番号

```latex
\pagestyle{plain}     % ページ番号のみ（デフォルト）
\pagestyle{empty}     % ページ番号なし
\pagestyle{headings}  % 見出しとページ番号
\pagenumbering{roman} % ローマ数字（i, ii, iii...）
\pagenumbering{arabic}% アラビア数字（1, 2, 3...）
```

### 5. URLの扱い

```latex
\usepackage{url}
\usepackage{xurl}
\urlstyle{same}                    % URLのスタイル
\def\UrlBreaks{\do\/\do-\do.\do?\do&\do=\do#}  % URL改行位置
```

### 6. 等幅フォントの改行設定

```latex
\makeatletter
\def\@texttt#1{%
  \begingroup
  \ttfamily
  \hyphenchar\font=`\-%
  \hyphenpenalty=0%
  \exhyphenpenalty=0%
  \tolerance=10000%
  \pretolerance=10000%
  \emergencystretch=10em%
  \sloppy
  #1%
  \endgroup
}
\makeatother
```

### 7. hyperrefの設定

```latex
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,        % リンクに色を付ける
  allcolors=blue,         % リンクの色
  breaklinks=true,        % リンクの改行を許可
  pdfborder={0 0 0},      % PDFのボーダー
  pdfstartview={FitH},    % PDFの開始表示
  pdfpagemode={UseOutlines}, % アウトライン表示
  unicode=true,           % Unicode対応
  pdftitle={論文タイトル}, % PDFのタイトル
  pdfauthor={著者名},     % PDFの著者
  pdfcreator={pdfLaTeX},  % PDFの作成者
  pdfproducer={LaTeX},    % PDFのプロデューサー
  hidelinks=false,        % リンクを隠すか
  linktocpage=false       % 目次のページ番号にリンク
}
```

---

## 🚫 LaTeXで絶対に避けるべき事項

### 重大な誤り

1. **データの改ざん・捏造**
   - 実験結果を都合よく変更する
   - 存在しないデータを作成する

2. **剽窃（Plagiarism）**
   - 他人の文章を無断で使用
   - 引用を明記せずに使用

3. **不正確な記述**
   - 数値の誤り
   - 統計的有意性の誤った解釈
   - 実験手順の不正確な記述

### LaTeX特有の誤り

1. **構文エラー**
   - `\begin{環境}`と`\end{環境}`の不一致
   - エスケープが必要な文字の未エスケープ（`&`, `%`, `$`, `#`, `^`, `_`, `{`, `}`, `~`, `\`）
   - 未定義のコマンドやパッケージ

2. **パッケージの競合**
   - 互いに競合するパッケージの同時使用
   - パッケージの読み込み順序の誤り

3. **コンパイルエラー**
   - エラーを無視してコンパイルを続行
   - 警告（warning）を無視

4. **図表の誤り**
   - `\label`がない図表
   - 本文中で参照されていない図表
   - 解像度が低い図（300dpi未満）

5. **数式の誤り**
   - 変数が定義されていない数式
   - 本文中で参照されていない重要な数式

6. **参考文献の誤り**
   - `\cite{label}`と`\bibitem{label}`の不一致
   - 引用形式の不統一

---

## 🔧 LaTeXコンパイル手順

### pdfLaTeXの場合

```bash
# 1回目のコンパイル
pdflatex main.tex

# BibTeXを使用している場合
bibtex main

# 2回目のコンパイル（参照を解決）
pdflatex main.tex

# 3回目のコンパイル（最終確認）
pdflatex main.tex
```

### LuaLaTeXの場合

```bash
# 1回目のコンパイル
lualatex main.tex

# BibTeXを使用している場合
bibtex main

# 2回目のコンパイル（参照を解決）
lualatex main.tex

# 3回目のコンパイル（最終確認）
lualatex main.tex
```

### エラー対処の基本原則

1. **エラーメッセージを読む**: エラーの原因を特定
2. **根本原因を特定**: 表面的な症状ではなく、根本原因を探る
3. **3回見直す**: エラー修正後、最低3回は異なる角度から確認
4. **段階的に修正**: 一度に複数の変更をしない
5. **バックアップを取る**: 修正前にバックアップを取る

---

## ✅ LaTeX最終チェックリスト

論文提出前に以下を必ず確認：

### 内容の確認
- [ ] すべての数値が正確か
- [ ] 統計的有意性が明記されているか（$p$値など）
- [ ] すべての主張に根拠（参考文献）があるか
- [ ] 文体が「である」調で統一されているか
- [ ] 口語体が混入していないか

### LaTeX構文の確認
- [ ] エラーなくコンパイルできるか
- [ ] 警告（warning）がないか（可能な限り）
- [ ] すべての`\begin{環境}`に対応する`\end{環境}`があるか
- [ ] エスケープが必要な文字が正しくエスケープされているか
- [ ] 未定義のコマンドがないか

### 図表の確認
- [ ] すべての図表に`\caption`があるか
- [ ] すべての図表に`\label`があるか
- [ ] すべての図表が本文中で`\ref{label}`で参照されているか
- [ ] 図表番号が連番か（自動生成を確認）
- [ ] 解像度が十分か（300dpi以上）

### 数式の確認
- [ ] 重要な数式に`\label{eq:name}`が付いているか
- [ ] 変数が定義されているか
- [ ] 本文中で`\eqref{eq:name}`で参照されているか
- [ ] 数式の構文エラーがないか

### 参考文献の確認
- [ ] 参考文献リストが完全か
- [ ] `\cite{label}`と`\bibitem{label}`が一致しているか
- [ ] 引用形式が統一されているか
- [ ] BibTeXを使用している場合、`.bib`ファイルが正しいか

### フォーマットの確認
- [ ] ページ番号が正しいか
- [ ] 目次が最新か（`\tableofcontents`を使用している場合）
- [ ] 章番号が正しいか
- [ ] 余白が適切か
- [ ] 2カラム形式の場合、カラム間隔が適切か

### 3回チェック
- [ ] **3回チェックを実施したか**
- [ ] エラー修正後、3回見直したか
- [ ] 異なる角度から確認したか

---

## 📌 重要な原則

1. **誠実性**: データと結果を正直に記述する
2. **再現性**: 他の研究者が再現できるように詳細に記述する
3. **明確性**: 読者が理解できるように明確に記述する
4. **一貫性**: 用語、記号、形式を統一する
5. **完全性**: 必要な情報をすべて含める
6. **LaTeX構文の正確性**: エラーなくコンパイルできるようにする

---

## 🎓 LaTeX特有のベストプラクティス

### 1. パッケージの整理

```latex
% 日本語処理
\usepackage[utf8]{inputenc}
\usepackage{zxjafont}  % または luatexja-preset

% 数式
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}

% 図表
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{float}

% レイアウト
\usepackage{geometry}
\usepackage{hyperref}
```

### 2. コマンドの定義

```latex
% よく使うコマンドを定義
\newcommand{\R}{\mathbb{R}}        % 実数
\newcommand{\N}{\mathbb{N}}        % 自然数
\newcommand{\E}{\mathbb{E}}        % 期待値
\newcommand{\Var}{\mathrm{Var}}    % 分散
```

### 3. 環境の定義

```latex
% 定理環境の定義
\newtheorem{theorem}{定理}
\newtheorem{lemma}{補題}
\newtheorem{definition}{定義}
```

### 4. バージョン管理

- `.tex`ファイルをGitで管理
- 重要な変更はコミット
- バックアップを定期的に取る

---

**最終確認**: このルールに従って執筆したLaTeX論文は、権威のある教授に見せても恥じない品質を保証します。エラーなくコンパイルでき、美しく整形されたPDFを生成できます。
